# Load the flake dev shell when nix-direnv is available; otherwise no-op so
# entering the directory never errors. The `just` recipes work either way.
if has nix_direnv_version; then
    use flake
else
    log_status "nix-direnv not found — use 'just <recipe>', or install nix-direnv for shell autoload."
fi
