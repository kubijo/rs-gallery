[package]
name = "gallery-build"
version.workspace = true
publish.workspace = true
edition.workspace = true
repository.workspace = true
license = "Unlicense"
description = "Build-script helper for gallery: discovers *.scene.rs by glob and emits their module includes."

[dependencies]
camino = "1"
glob = "0.3"
serde = { version = "1", features = ["derive"] }
toml = "1"
